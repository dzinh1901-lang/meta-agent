# Next Phase Backlog

Status date: 2026-06-24

## P0 — Current connected portfolio

1. AgentOps Runtime: review the GTC runtime-control branch, then bind each approval receipt to the task digest, policy version, actor, expiry, and allowed action set before worker dispatch.
2. Meridian Yacht Atelier: complete open Issue #5 with a pure domain core, provider-neutral ports, typed contracts, connector registry, deterministic routing policies, and fake connectors and agents before adding another live provider.
3. AURELEAN: complete hosted deployment evidence, production identity and RBAC, target database validation and cutover, tenant ownership, and observability before enabling live AI integrations.
4. Meta-Agent: add read-only GitHub discovery for repository access, default branch and head, open issues and pull requests, evidence age, blockers, and confidence.
5. Keep the GTC review branches free of live credentials, provider activation, paid execution, deployment, purchases, and external communication.

## P1 — Secure execution enforcement

1. Define the secure-runtime adapter contract beneath AgentOps Runtime.
2. Translate normalized sandbox, filesystem, network, process, delegation, credential-reference, and resource-budget policy into a deterministic fake adapter first.
3. Add a staging-only OpenShell or NemoClaw adapter only after fail-closed tests exist.
4. Resolve credential references inside the gateway and redact sensitive values from records and logs.
5. Store approval and policy decisions as append-only events with a separate current-state projection.
6. Meter wall time, actions, retries, provider usage, tokens where applicable, and cost.

## P2 — Provider-neutral model and skill evaluation

1. Add a model registry and routing contract independent of provider names.
2. Build reproducible fixtures for orchestration, repository review, sourcing and RFQ work, document analysis, and cinematic planning.
3. Benchmark Nemotron 3 Ultra against current candidates for quality, structured-output validity, long-horizon completion, tool use, policy adherence, latency, reliability, and unit cost.
4. Evaluate CUDA-X skills only through registered and versioned capability contracts: `cuOpt` for constrained sourcing or scheduling, `cuDF` when data scale warrants GPU acceleration, and AI-Q or NeMo for routing, evaluation, governance, and enterprise-agent workflows.
5. Keep a fallback path for every provider-specific capability.

## P3 — Domain-specific application work

1. AURELEAN: define optimization objectives, constraints, benchmark data, explainability, and human award controls before a `cuOpt` proof of concept.
2. Meridian: finish connector-neutral Phase 1, then evaluate Cosmos 3 as one optional adapter for spatial continuity or physical-AI media generation.
3. Keep RTX Spark optional for local and private development; do not make application behavior depend on the device.
4. Preserve existing human approval and publication gates for supplier awards, client delivery, cinematic take selection, and public release.

## P4 — Infrastructure decision

Do not begin DSX, Vera Rubin, or AI-factory planning until a separately approved capacity review proves sustained workload demand, unmet latency or privacy requirements, expected utilization, operating cost, staffing and recovery needs, and a buy-versus-cloud business case.

## Evidence and connection blockers

1. Reconnect or provide direct repository evidence for VDS DesignOS before routing work or reporting current status.
2. Confirm and connect the canonical Monsieur repository before treating it as an executable project.
3. Record inaccessible or owner-dependent work as blocked; do not substitute old local-path statements for current repository evidence.
